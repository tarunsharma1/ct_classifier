# Here's where you define experiment-specific hyperparameters.
# You can also create lists and group parameters together into nested sub-parts.
# In Python, this is all read as a dict.

# environment/computational parameters
seed: 32678456782       # random number generator seed (long integer value)
device: cpu
num_workers: 4

# dataset parameters
data_root: /home/tsharma/tarun_code/10_plus_5_percent_crops
num_classes: 50
train_label_file: /home/tsharma/tarun_code/10_percent_labels.csv
val_label_file: /home/tsharma/tarun_code/5_percent_labels.csv

# training hyperparameters
image_size: [224, 224]
num_epochs: 200
batch_size: 128
learning_rate: 0.001
weight_decay: 0.001